import { UiTabPanelDirective } from './ui-tab-panel.directive';

describe('UiTabPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new UiTabPanelDirective();
    expect(directive).toBeTruthy();
  });
});
