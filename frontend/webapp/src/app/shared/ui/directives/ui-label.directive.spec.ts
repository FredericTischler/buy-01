import { UiLabelDirective } from './ui-label.directive';

describe('UiLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new UiLabelDirective();
    expect(directive).toBeTruthy();
  });
});
